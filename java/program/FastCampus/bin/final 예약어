final 예약어

final 변수는 값이 변경될수 없는 상수이다.
public static final double PI = 3.14;
오직 한번만 값을 할당할수있다.

final 메소드는 하위 클래스에서 재정의(overriding) 할수 없다
final 클래스는 더이상 상속되지 않는다.
java의 String 클래스 

public class Define {
	public static final int MIN = 1; // 상수
	
	상수는 static final 로 쓰인다.
	클래스로 이름 으로 부터 가져와서 쓸수 있다 
	
	프로젝트 구현 시 여러 파일에서 공유해야 하는 상수 값은 하나의
	파일에 선언하여 사용하면 편리하다
	
}
